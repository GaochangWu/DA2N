You should prepare your own .h5 format training and testing datasets here (e.g. "train.h5" and "test.h5").
The data shape should be batch (example) x channel x width x angular.

Make .h5 training datasets using provided codes.
1. Use "extract_EPI_from_XXX.m" to extract EPIs.
2. Use "generate_h5.m" to make .h5 datasets.
